name: Build
on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:
jobs:
  cdk:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      issues: read
      checks: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.GU_RIFF_RAFF_ROLE_ARN }}
          aws-region: eu-west-1

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"
          cache-dependency-path: cdk/package-lock.json
      - run: npm ci
        working-directory: cdk
      - run: npm test
        working-directory: cdk
      - run: npm run synth
        working-directory: cdk
        env:
          BUILD_NUMBER: ${{ github.run_number }}

      - name: Upload to Riff-Raff
        uses: guardian/actions-riff-raff@v2
        with:
          projectName: Content Platforms::content-audit
          configPath: ./cdk/cdk.out/riff-raff.yaml
          contentDirectories: |
            cdk.out:
              - ./cdk/cdk.out/ContentAudit-euwest-1-CODE.template.json
              - ./cdk/cdk.out/ContentAudit-euwest-1-PROD.template.json

  build-playwright-image:
    runs-on: ubuntu-24.04-arm
    permissions:
      # Needed to interact with GitHub's OIDC Token endpoint to configure AWS credentials
      id-token: write
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: lts/*

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-region: eu-west-1
        role-to-assume: ${{secrets.GU_ECR_ROLE_ARN}}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to ECR
      uses: aws-actions/amazon-ecr-login@v2
      id: ecr-login
      with:
        mask-password: "true"
      continue-on-error: true

    - name: Build and push
      uses: docker/build-push-action@v5
      env:
        REGISTRY: ${{ steps.ecr-login.outputs.registry }}
        IMAGE_TAG: ${{ github.run_number }}
      with:
        context: projects/playwright-runner
        platforms: linux/arm64
        push: true
        tags: ${{ env.REGISTRY }}/content-audit/playwright-runner:${{ env.IMAGE_TAG }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        provenance: false
